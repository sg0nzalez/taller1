/*
    string texto;
    crear_string(texto);

    cargar_string(texto);

    arreglo_string params;
    params.tope = 0;
    separar_por_espacio(texto, params);

    int i = 0;
    printf("\nTOPE = %d\n", params.tope);

    for(int i=0;i<params.tope;i++){
        printf("\n %d \n", i);
        desplegar_string(params.arre[i]);
        printf("\n %d \n", i);
    }
*/
